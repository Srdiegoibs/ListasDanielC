// 24)Escreva um programa que receba três entradas, um valor, uma posição de bit e uma operação.
// O programa deve executar a operação escolhida sobre o valor:
// a) Escrever um bit a posição indicada.
// b) Apagar um bit na posição indicada.
// c) Ler um bit na posição indicada
#include <stdio.h>

int main () {
    int a, b, c;
    scanf ("%d%d%d", &a, &b, &c);
    printf ("")
    return 0;
}